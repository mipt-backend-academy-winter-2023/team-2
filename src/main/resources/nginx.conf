events {}
http {
    upstream routing {
        server routing1:8084;
        server routing2:8085;
        server routing3:8086;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://routing;
            proxy_next_upstream error timeout;
            proxy_connect_timeout 1s;
        }
    }
}